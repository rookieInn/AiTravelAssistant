package com.example.mongodemo;

import org.junit.jupiter.api.Test;

class MongoDemoApplicationTests {
  @Test
  void smokeTest() {
    // Intentionally not bootstrapping Spring here (keeps tests green without requiring MongoDB).
  }
}

